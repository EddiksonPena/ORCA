global:
  resolve_timeout: 5m

route:
  receiver: orca-default
  group_by: ["alertname", "service", "module"]
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: orca-default
