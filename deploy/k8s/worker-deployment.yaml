apiVersion: apps/v1
kind: Deployment
metadata:
  name: orca-worker
  namespace: orca
  labels:
    app: orca-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orca-worker
  template:
    metadata:
      labels:
        app: orca-worker
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: ghcr.io/orca/orca-worker:latest
          ports:
            - name: http
              containerPort: 4010
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
          envFrom:
            - configMapRef:
                name: orca-config
          env:
            - name: NEO4J_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: orca-secrets
                  key: neo4j-password
            - name: ORCA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: orca-secrets
                  key: api-key
          readinessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4010
            initialDelaySeconds: 15
            periodSeconds: 15
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1.5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: orca-worker
  namespace: orca
  labels:
    app: orca-worker
spec:
  selector:
    app: orca-worker
  ports:
    - name: http
      port: 4010
      targetPort: 4010
